import {Header} from "../Header";
import {Hero} from "../Hero";
import {Form} from "../Form";

const App = () => {
    return (
        <>
            <Header/>
            <Hero/>
            <Form/>
        </>
    );
};

export {App};
