import {Header} from "../Header";
import {Hero} from "../Hero";
import {Form} from "../Form";
import {Features} from "../Features";

const App = () => {
    return (
        <>
            <Header/>
            <Hero/>
            <Form/>
            <Features/>
        </>
    );
};

export {App};
